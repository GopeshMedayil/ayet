
/**
 * Component to render the header
 * @returns 
 */
const Header = () => {
    return (
        <header className="h-32 w-full text-white bg-gradient-to-r from-red-500 via-pink-600 to-blue-600 ">
            <h1 className="font-bold p-3 text-xl">Choose a survey to earn virtual currency</h1>
        </header>
    )
}

export default Header;